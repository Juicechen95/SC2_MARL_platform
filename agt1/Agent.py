class Agent:
    def __init__(self, Config):
        # Config.var
        pass

# set up policy network
# under specific scope (recommended 'pol1')

# restore model if provided
# model stored in ./model/model.ckpt.meta
# restore from scope('pol1') to scope('pol1')
# model/pol1 or model/pol2

# act()
# action, values[step] = self.agent.act(self.state)

# example: hold agent in agt1 and built AI in agt2
